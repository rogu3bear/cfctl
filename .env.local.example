# Retired. cfctl v2 reads no repo-local env override file — .env.local is
# not consumed by the binary. See .env.example for the real v2 environment
# surface and docs/v1-parity.md for the retired v1 contract.
