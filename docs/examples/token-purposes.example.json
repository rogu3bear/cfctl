{
  "_readme": "Consumer-owned purposes file for `cfctl token rotate --purposes-file`. The consumer declares WHAT each scoped child may do (name, TTL, Cloudflare token policy); cfctl owns the lifecycle (rotate decision, mint, state, sweep). Resolve permission-group ids with `cfctl token permission-groups`. Replace <ACCOUNT_ID> and the <...GUID> placeholders.",
  "purposes": [
    {
      "purpose": "cf-founder-deploy",
      "name_prefix": "cf-founder-deploy",
      "ttl_days": 7,
      "policies": [
        {
          "effect": "allow",
          "resources": {
            "com.cloudflare.api.account.<ACCOUNT_ID>": "*"
          },
          "permission_groups": [
            { "id": "<Workers Scripts Write GUID>" }
          ]
        }
      ]
    },
    {
      "purpose": "cf-workers-ai",
      "name_prefix": "cf-workers-ai",
      "ttl_days": 30,
      "policies": [
        {
          "effect": "allow",
          "resources": {
            "com.cloudflare.api.account.<ACCOUNT_ID>": "*"
          },
          "permission_groups": [
            { "id": "<Workers AI Read GUID>" }
          ]
        }
      ]
    }
  ]
}
